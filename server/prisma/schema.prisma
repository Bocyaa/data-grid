generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Dataset {
  id Int @id @default(autoincrement())
  name String
  createdAt DateTime @default(now())
  rows Json // stores array of objects
}